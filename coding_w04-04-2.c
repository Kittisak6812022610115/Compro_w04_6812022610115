#include <stdio.h>

int main() {
    float score = 90.5f;
    printf("score: %.1f\n", score);
    return 0;
}

// code ทำงานได้ปกติเพราะ 90.5 เป็นค่า double และใส่ F ด้านหลัง 90.5 เพื่อระบุว่าเป็น Float